# ─────────────────────────────────────────────
#  requirements.txt  –  ETL Climate Pipeline
# ─────────────────────────────────────────────

# HTTP
requests==2.32.3

# Data manipulation
pandas==2.2.3
numpy==1.26.4           # dependência indireta do pandas (fixada para estabilidade)

# Parquet I/O
pyarrow==16.1.0

# Opcional – leitura de Parquet via Dask (para grandes volumes)
# dask[dataframe]==2024.8.0

# Desenvolvimento / qualidade de código (não necessário em produção)
# pytest==8.3.2
# ruff==0.5.7
# mypy==1.11.1
